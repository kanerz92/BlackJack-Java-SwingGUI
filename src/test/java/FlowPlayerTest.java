import org.junit.jupiter.api.Test;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.BDDMockito.given;
import static org.mockito.Mockito.*;

public class FlowPlayerTest {
    @Test
    public void handToScoreIsCalled() {

//        // given a hand of a player
//        HandToScore handToScore  = mock(HandToScore.class);
//        given(handToScore.scoreHand(any())).willReturn(1);
//        iDeck mockDeck = mock(Deck.class);
//        ConsoleInputGetter consoleInputGetter = mock(ConsoleInputGetter.class);
//        iDealer mockDealer = mock(iDealer.class);
//        FlowPlayer flowPlayer = new FlowPlayer(handToScore, consoleInputGetter, mockDealer);
//        // when i call my player flow with Hit command
//        Hand hand = new Hand();
//        flowPlayer.player_flow(hand,mockDeck);
//        // then handToScore is called once
//        verify(handToScore, times(1)).scoreHand(new int[]{1,2});
    }
}
